<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ja.finalproject.user.mapper.UserSqlMapper">
    <insert id="createUser">
        insert into fp_user(
            account_name, password, nickname, 
            email, gender, birth, phone
        )
        values(
            #{account_name}, #{password}, #{nickname},
            #{email}, #{gender}, #{birth}, #{phone}
        )
    </insert>

    <!-- select 꼭 resultType 알려줘야됨-->
    <select id="findUserByAccountNameAndPassword" resultType="com.ja.finalproject.dto.UserDto">
        select * from fp_user 
        where account_name = #{account_name}
        and password = #{password}
    </select>

    <select id="findUserById" resultType="com.ja.finalproject.dto.UserDto">
        select * from fp_user where id = #{id}
    </select>


</mapper>


